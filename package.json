{
	"name": "nuxt-app",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "nuxt build",
		"dev": "env-cmd -f .env.local nuxt dev",
		"proddev": "env-cmd -f .env.local nuxt dev --https --ssl-cert nexon.com.pem --ssl-key nexon.com-key.pem --port 443 --host supervive.nexon.com",
		"generate": "nuxt generate",
		"preview": "nuxt preview",
		"postinstall": "nuxt prepare"
	},
	"dependencies": {
		"@azure/storage-blob": "^12.25.0",
		"env-cmd": "^10.1.0",
		"sass": "^1.79.5",
		"vue": "latest"
	},
	"devDependencies": {
		"nuxt": "^3.13.2"
	}
}
